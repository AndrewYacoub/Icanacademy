<% if @single_room && @single_room.name == room.name%>
  <% isActive =  'active' %>
<% else %>
  <% isActive = '' %>
<% end %>
<%= link_to room.name,
 course_group_room_path(room.group.course, room.group, room),
  class:'list-group-item room_list_item ' + isActive %>